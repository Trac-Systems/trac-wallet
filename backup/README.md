This folder stores the pre-TypeScript JavaScript implementation and tests for reference.

- `index.js` and `constants.js` are the previous JS entry points.
- `test/` contains the previous JS test suite.
- `dist/` contains the previous compiled artifacts.
